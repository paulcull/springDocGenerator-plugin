# Docs Generator Module Usage (`springdoc-docs:generate-docs`)

This module (`springdoc-sdk-plugin-docs-generator`) provides the `springdoc-docs:generate-docs` goal.

**(Details about this module's specific functionality, configuration options, and use cases need to be added here based on its implementation.)**

It is expected that this module might offer alternative documentation formats, different template engines, or specialized documentation generation features compared to the `core` module.

Consult the source code of the `GenerateDocsMojo` (or equivalent) within the `docs-generator` module for specific parameters and behavior. 